/*globals makePathway*/
// <%= library %> manifest
makePathway("<%= library %>", window)('__manifest__', function () {
	'use strict';
	return {
		files: <%- JSON.stringify(files) %>,
		packages: <%- JSON.stringify(packages) %>
	};
});