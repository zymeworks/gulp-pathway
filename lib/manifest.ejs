/*globals makePathway*/
// <%= library %> manifest
(<%- pathwaySrc %>)("<%= library %>", window)('__manifest__', function () {
	'use strict';
	return {
		files: <%- JSON.stringify(files) %>,
		packages: <%- JSON.stringify(packages) %>
	};
});