<%- global %>['@<%- library %>'](<%- (!isRootPackage ? "'" + package + "', " : '') %>function ($import, $package) {
	<% if (strict) { %>'use strict';<% } else { %><%- '// strict mode disabled!' %><% } %>
	//
	<%- content.split('\n').join('\n	') %>
	//<% if (exports.length) { %> $exports
	return {
	<%- exports.map(function (exp) {
	return '	' + exp.exported + ': ' + exp.local;
	}).join(",\n	") %>
	};<% } %>
});